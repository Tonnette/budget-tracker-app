var compression=require("compression");const express=require("express"),mongoose=require("mongoose"),PORT=process.env.PORT||3e3,app=express();app.use(compression()),app.use(express.urlencoded({extended:!0})),app.use(express.json()),app.use(express.static("public")),mongoose.connect(process.env.MONGODB_URI||"mongodb://localhost/budget",{useNewUrlParser:!0,useFindAndModify:!1}),app.use(require("./routes/api.js")),app.listen(PORT,()=>{console.log(`App running on port ${PORT}!`)});